// Place your settings in this file to overwrite default and user settings.
{
	"files.exclude": {
		// exclude the js when a matching ts in the same folder
        "**/*.js": { "when": "$(basename).ts"}
    }
}