install package "atom-typescript", if using atom editor "apm install atom-typescript"

to install tsd:
  npm install -g tsd

to add types for angular to typings folder:
  tsd install angular --resolve --save

to add types after getting from source control:
  tsd install

--------------------------------

